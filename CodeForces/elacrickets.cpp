#include <bits/stdc++.h>

using namespace std;
using i64 = long long;

void solve() {
    int n; cin >> n;
    vector<int> r (3), c (3);
    for (int i = 0; i < 3; ++i) {
        cin >> r[i] >> c[i];
    }
    int c = -1;
    for (int i = 0; i < n; ++i) {
        
    }
}

signed main()
{
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int T; cin >> T;
    while(T--) solve();
    return 0;
}
